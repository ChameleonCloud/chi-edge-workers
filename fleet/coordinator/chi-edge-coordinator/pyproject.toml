[project]
name = "chi-edge-coordinator"
dynamic = ["version"]
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "keystoneauth1",
    "lockfile",
    "requests",
]

[project.optional-dependencies]
dev = [
    "ruff",
]
